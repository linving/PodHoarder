<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/fragment_latest_background"
    android:id="@+id/episodesFragment_container">

    <TextView
        android:id="@+id/episodesTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingLeft="15dip"
        android:text="@string/episodes_title"
        android:textColor="@color/fragment_latest_title"
        android:textSize="@dimen/fragment_latest_title"/>
    
    <ListView
        android:id="@+id/mainListView"
        android:paddingTop="5dp"
        android:layout_marginLeft="15dip"
        android:layout_marginRight="15dip"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:divider="@drawable/list_divider"
        android:dividerHeight="1px"/>

</LinearLayout>